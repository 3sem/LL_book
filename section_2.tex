\chapter{Практические задания}

\section{Задания по теоретическому блоку}
{\bf\large $\blacktriangle$ Для самостоятельного решения}
\begin{enumerate}
	\item \textbf{a)} Приведите пример $\text{LL}(0)$ грамматики \\
	\textbf{b)} Приведите пример $\text{LL}(2)$, но не $\text{LL}(1)$ грамматики.\\
	\textbf{с)} Приведите не $\text{LL}(2)$ грамматики.
	\item Для грамматик из задачи 1 постройте LL-анализаторы и разберите слово длины хотя бы 5 символов.
	\item 	Является ли грамматика $G$ LL(1) грамматикой:
	\begin{align*}
		G: & \\
		S \vdash A|B & \\
		A \vdash aA|c & \\
		B \vdash aB|b
	\end{align*}
	%
	%Рассмотрим два шага разных левых порождений строки, начинающейся с $a$ в $G$:
	%\begin{enumerate}
	%	\item $\underline{S} \vdash_{lm} \underline{A} \vdash_{lm} {aA} \vdash_{lm} \cdots$ 
	%	\item $\underline{S} \vdash_{lm} \underline{B} \vdash_{lm} {aB}\vdash_{lm} \cdots$
	%\end{enumerate}
	
	%Вычислим $FIRST$.
	%\begin{align*}
	%	FIRST(A)  &= \{ a, c \} & \\
	%	FIRST(B) &= \{ a, b \}
	%\end{align*}
	
	%В виду того, что $FIRST(A)\cap{FIRST(B)} \neq %\varnothing$, нельзя однозначно произвести %подчёркнутый шаг левого порождения, и %рекурсивный спуск неприменим.
	\item{ В литературе по рекурсивному спуску иногда приводят так называемый канонический вид грамматики для рекурсивного спуска:
	
	{\bf$\blacktriangle$ Определение 13.10.}\label{canonical_rd} Грамматика $G = \langle \Sigma, N, S, P \rangle$ и $\omega \in \Sigma^{*}$ называется грамматикой в \textit{каноническом виде для рекурсивного спуска}, если её правила удовлетворяют следующему виду:
	\begin{itemize}
		\item либо $A\vdash \alpha, \alpha \in (N\cup{\Sigma})^*$ --- единственное правило вывода для $A \in P$ 
		\item либо, $A\ \vdash a_1\alpha_1|\dots|a_m\alpha_m: \alpha_i \in (N\cup{\Sigma})^* , a_i \in \Sigma,  \forall i=1\dots{m}$, и $\alpha_i \neq \alpha_j$, если $i \neq j$
		\item либо $A\ \vdash a_1\alpha_1|\dots|a_m\alpha_m|\varepsilon: \alpha_i \in (N\cup{\Sigma})^* , a_i \in \Sigma,  \forall i=1\dots{m}$, и $\alpha_i \neq \alpha_j$, если $i \neq j$, и $FIRST(A)\cap{FOLLOW(A)}=\varnothing$.
	\end{itemize}
	Докажите, что грамматики, заданные определением, являются LL(1).
	
	Следует отметить, что в реализациях безоткатного рекурсивного спуска\footnote{не говоря уже о рекурсивном спуске с откатом} для реальных языков программирования условия из задачи \ref{canonical_rd} могут нарушаться.}
	\item 	Рассмотрим классический пример \verb|'if, if-else'| в реализации некоторых языков программирования:
	\begin{align*}
		S\ \vdash \ if\ E:\ S | if\ E:\ S\ else:\ S | a \\
		E \vdash b
	\end{align*}
	
	После левой факторизации грамматика имеет вид:
	
	\begin{align*}
		S \vdash if\ E:\ SS' | a \\
		S' \vdash else:\ S | \varepsilon \\
		E \vdash b
	\end{align*}
	
	
	Самостоятельно проведите левую факторизацию. Является ли полученная грамматика LL(1) грамматикой?
\end{enumerate}	


\section {Разработка LL(1)-анализатора на языке Python}
{\bf\large $\blacktriangle$ Задача для программирование}
\begin{enumerate}
	\item Реализуйте $LL(1)$ анализатор.
\end{enumerate}




